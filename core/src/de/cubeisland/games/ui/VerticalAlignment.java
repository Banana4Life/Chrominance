package de.cubeisland.games.ui;

public enum VerticalAlignment {
    /**
     * Keeps the widget at the top most position
     */
    TOP,

    /**
     * Keeps the widget vertically centered within the parent
     */
    MIDDLE,

    /**
     * keeps the widget at the bottom most position
     */
    BOTTOM
}
