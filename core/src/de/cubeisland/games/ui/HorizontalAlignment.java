package de.cubeisland.games.ui;

public enum HorizontalAlignment {
    /**
     * Keeps the widget to the left most position
     */
    LEFT,

    /**
     * Keeps the widget centered in the body of the parent widget
     */
    CENTER,

    /**
     * Keeps the widget to the right most position
     */
    RIGHT
}
